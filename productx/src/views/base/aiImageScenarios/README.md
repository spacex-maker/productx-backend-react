# AI 生图场景配置管理前端页面

## 📋 概述

本模块为 AI 生图场景配置提供完整的前端 CRUD 管理界面，用于管理和配置不同的 AI 图像生成场景。

## 📂 文件结构

```
aiImageScenarios/
├── AiImageScenarios.js                    # 主页面组件
├── AiImageScenariosTable.js               # 表格组件
├── AiImageScenariosCreateFormModel.js     # 创建表单弹窗
├── UpdateAiImageScenariosModel.js         # 更新表单弹窗
└── README.md                              # 本文档
```

## 🚀 主要功能

### 1. 场景列表查询
- ✅ 分页查询
- ✅ 多条件筛选（场景标识、场景名称、分类、启用状态、VIP状态）
- ✅ 实时搜索

### 2. 场景创建
- ✅ 基本信息配置（标识、名称、描述、图标、封面）
- ✅ 提示词配置（正向提示词模板、负向提示词）
- ✅ 生成参数配置（底模、尺寸、CFG Scale、采样步数）
- ✅ 高级配置（LoRA 配置、表单配置）
- ✅ 状态设置（启用/禁用、VIP专属）

### 3. 场景编辑
- ✅ 修改所有场景配置项
- ✅ 保留场景标识的提示（不建议修改）

### 4. 场景删除
- ✅ 单个删除（带确认提示）
- ✅ 删除前确认

### 5. 状态管理
- ✅ 快速启用/禁用切换
- ✅ 实时状态更新

## 🎨 界面特性

### 搜索筛选区
- 场景标识输入框
- 场景名称输入框
- 场景分类下拉选择（商业/个人/艺术/通用）
- 启用状态下拉选择
- VIP专属下拉选择
- 搜索按钮
- 创建场景按钮

### 数据表格
| 列名 | 说明 | 特殊显示 |
|------|------|----------|
| 勾选框 | 用于批量操作 | 复选框 |
| 场景标识 | 唯一标识 | 青色标签 |
| 场景名称 | 显示名称 | 文本 |
| 场景分类 | 分类类型 | 彩色标签 |
| 排序权重 | 显示顺序 | 数字 |
| 默认尺寸 | 宽×高 | 文本 |
| VIP专属 | 是否VIP | 金色/普通标签 |
| 启用状态 | 当前状态 | 开关按钮 |
| 创建时间 | 创建时间 | 日期时间 |
| 更新时间 | 更新时间 | 日期时间 |
| 操作 | 操作按钮 | 编辑/删除 |

### 创建/编辑表单

表单采用分组设计，包含以下部分：

#### 基本信息
- **场景标识** - 必填，用于代码引用（如：BILLBOARD）
- **场景名称** - 必填，显示名称（如：户外高清广告牌）
- **场景描述** - 选填，用于前端展示
- **场景分类** - 选填，默认为"通用"
- **排序权重** - 选填，默认为 0，值越大越靠前
- **场景图标URL** - 选填
- **封面预览图URL** - 选填

#### 提示词配置
- **提示词模板** - 支持变量占位符（如：`A billboard of ${subject}, ${style}`）
- **负向提示词** - 用于避免不良生成结果

#### 生成参数
- **默认底模ID** - 如：sd_xl_base_1.0
- **默认宽度** - 必填，范围 64-4096，默认 1024
- **默认高度** - 必填，范围 64-4096，默认 1024
- **提示词相关性** - CFG Scale，范围 1-20，默认 7.0
- **采样步数** - 范围 1-150，默认 20

#### 高级配置
- **LoRA配置** - JSON格式，示例：
  ```json
  [{"model":"lora_v1","weight":0.8}]
  ```
- **表单配置** - 前端动态表单定义，JSON格式，示例：
  ```json
  [{"field":"subject","label":"主题","type":"text"}]
  ```

#### 状态设置
- **启用状态** - 开关，默认启用
- **VIP专属** - 开关，默认非VIP

## 💡 使用示例

### 创建商业广告牌场景

1. 点击"创建场景"按钮
2. 填写表单：
   - 场景标识：`BILLBOARD`
   - 场景名称：`户外高清广告牌`
   - 场景描述：`将产品放置在真实的户外广告牌上`
   - 场景分类：选择"商业"
   - 排序权重：`100`
   - 提示词模板：`A realistic outdoor billboard displaying ${product}, ${environment} background, professional photography`
   - 负向提示词：`low quality, blurry, distorted, cartoon`
   - 默认尺寸：`1024 × 1024`
   - CFG Scale：`7.0`
   - 采样步数：`20`
   - 表单配置：
     ```json
     [
       {"field":"product","label":"产品描述","type":"text"},
       {"field":"environment","label":"环境","type":"select","options":["city street","highway","shopping mall"]}
     ]
     ```
3. 点击"确认"完成创建

### 创建VIP专属头像场景

1. 点击"创建场景"按钮
2. 填写表单：
   - 场景标识：`AVATAR_PRO`
   - 场景名称：`职业头像`
   - 场景描述：`生成专业的商务头像`
   - 场景分类：选择"个人"
   - 提示词模板：`Professional portrait photo, ${gender} person, ${style} style, studio lighting`
   - 默认尺寸：`512 × 768`
   - **VIP专属：开启**
3. 点击"确认"完成创建

## 🔧 技术实现

### API 接口调用

| 操作 | 接口 | 方法 |
|------|------|------|
| 列表查询 | `/manage/ai-image-scenarios/list` | GET |
| 创建 | `/manage/ai-image-scenarios/create` | POST |
| 更新 | `/manage/ai-image-scenarios/update` | POST |
| 删除 | `/manage/ai-image-scenarios/delete` | DELETE |
| 状态切换 | `/manage/ai-image-scenarios/change-status` | POST |

### 组件依赖

```javascript
// 主要依赖
import { Form, Input, Select, Switch, InputNumber, Divider, Modal, Button, Table } from 'antd'
import { useTranslation } from 'react-i18next'
import api from 'src/axiosInstance'

// 公共组件
import { UseSelectableRows } from 'src/components/common/UseSelectableRows'
import { HandleBatchDelete } from 'src/components/common/HandleBatchDelete'
import Pagination from "src/components/common/Pagination"
```

## 🎯 数据验证

### 创建时必填项
- ✅ 场景标识（sceneKey）
- ✅ 场景名称（sceneName）
- ✅ 默认宽度（width）
- ✅ 默认高度（height）

### 更新时必填项
- ✅ ID（id）
- ✅ 场景标识（sceneKey）
- ✅ 场景名称（sceneName）

## 📱 响应式设计

- 表单采用栅格布局（Ant Design Grid）
- 支持不同屏幕尺寸自适应
- 弹窗宽度设置为 900px，适合大部分表单内容

## 🎨 UI/UX 特性

### 视觉标识
- **场景标识**：使用青色标签突出显示
- **场景分类**：不同分类使用不同颜色标签
  - 商业：蓝色
  - 个人：绿色
  - 艺术：紫色
  - 通用：默认色
- **VIP专属**：金色标签
- **启用状态**：开关组件，直观显示

### 交互提示
- 表单字段带有 tooltip 提示
- 删除操作带有确认弹窗
- 操作成功/失败有消息提示
- 加载状态显示 Spin 组件

## 🔒 注意事项

1. **场景标识唯一性**
   - 创建时会校验场景标识是否已存在
   - 更新时建议不要修改场景标识（前端代码可能引用）

2. **JSON 格式字段**
   - `loraConfig` 和 `formConfig` 必须是有效的 JSON 格式
   - 建议使用 JSON 格式化工具检查

3. **图片 URL**
   - 建议使用 CDN 地址
   - 确保图片可访问

4. **删除操作**
   - 为物理删除，请谨慎操作
   - 建议改为禁用状态而非删除

## 🔄 路由配置

需要在项目路由配置中添加以下路由：

```javascript
{
  path: '/base/ai-image-scenarios',
  name: 'AI生图场景配置',
  component: () => import('@/views/base/aiImageScenarios/AiImageScenarios.js'),
}
```

## 🌐 国际化支持

页面使用 `react-i18next` 进行国际化，需要添加以下翻译键：

```json
{
  "场景标识": "Scene Key",
  "场景名称": "Scene Name",
  "场景分类": "Category",
  "商业": "Commercial",
  "个人": "Personal",
  "艺术": "Art",
  "通用": "General",
  "启用状态": "Active Status",
  "VIP专属": "VIP Only",
  "创建场景": "Create Scene",
  "编辑场景配置": "Edit Scene",
  "排序权重": "Sort Order",
  "默认尺寸": "Default Size",
  "提示词模板": "Prompt Template",
  "负向提示词": "Negative Prompt",
  "LoRA配置": "LoRA Config",
  "表单配置": "Form Config",
  "生成参数": "Generation Parameters",
  "高级配置": "Advanced Config",
  "状态设置": "Status Settings"
}
```

## 📝 开发建议

1. 测试场景标识唯一性校验
2. 测试 JSON 字段的验证
3. 测试分页和排序功能
4. 测试状态切换的实时更新
5. 测试删除操作的确认流程

---

**创建日期：** 2025-01-09  
**作者：** Anakki  
**版本：** 1.0.0
